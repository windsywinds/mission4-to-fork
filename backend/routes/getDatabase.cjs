const express = require("express");
const router = express.Router();

//You'll need to import some stuff here



//You'll need to make a connection to the database



// A model based on the schema?



// Finished the route that will get the cars and send them back to the front end
router.get("/", async (req, res) => {

   

  });

module.exports = router;
